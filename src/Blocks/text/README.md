# Text Block

**Pattern demonstrated:** Minimal text content with style variants.

The text block renders rich-text content as body text. It shows how to:

- Use `text` with the `body` style variant
- Bind to a richtext attribute via `{{ attributes.content }}`

This is the simplest possible renderer description — a single element with one attribute binding. Start here if you're new to renderer descriptions.
